SET(ICON_FILES
	sdl_instead.png
	sdl_instead.svg
	sdl_instead_1.png
)

IF(SYSTEMWIDE)
	INSTALL(FILES ${ICON_FILES} DESTINATION ${ICONDIR})
ELSEIF(STANDALONE)
	INSTALL(FILES ${ICON_FILES} DESTINATION ${STANDALONEDIR}/icon)
ENDIF(SYSTEMWIDE)
